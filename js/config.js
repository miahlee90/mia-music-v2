/* LMS configuration — fill these two values in after creating the Supabase
   project (Project Settings → API). Only the URL and the PUBLISHABLE (anon)
   key belong here. NEVER put the service-role key, database password, or any
   secret key in this file — it is public on the website.
   Leave both empty to run the site with the LMS completely disabled. */
const LMS_CONFIG={
  SUPABASE_URL:"https://aeojiauqujttnqhmvurh.supabase.co",
  SUPABASE_ANON_KEY:""    /* the long "anon / publishable" key */
};
